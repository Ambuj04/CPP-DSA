 // while (left <= right) {
    //     int mid = left + (right - left) / 2;
    //     if (a[mid] == x) return mid;
    //     else if (a[mid] < x) left = mid + 1;
    //     else right = mid - 1;
    // }